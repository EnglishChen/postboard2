<%= render 'shared/title', title: 'Post List'%>

<% @posts.each do |post| %>
  
  <div class="row">
    <div class="well col-md-6">
      <p>
        <h2><%= post.title %></h2>
      </p>
      <p>
        <%= post.content %>
      </p>
      <br>
      <span>
        <%= link_to "show this post", post_path(post) %>
      </span>
    </div>
  </div>

<% end %>
